# Devin Git Workflow

Git is part of the development process, not an afterthought.

---

## Before Starting

Always inspect the repository state before meaningful work.

Use:

```bash
git status
git branch --show-current
git log --oneline -10